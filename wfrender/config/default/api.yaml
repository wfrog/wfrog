
renderer: !template
    path: ../../templates/default/api.txt
    mime: text
    renderer: !data
        source: !accumulator
            slice: minute
            span: 180
            storage: !service
                name: storage
        renderer: !multi
            children:
                current: !include
                    path: current.yaml